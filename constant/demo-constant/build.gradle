dependencies {
    compile 'com.cmpay:cmpay-interface'
}

jar {
    manifest {
        attributes(
                'Implementation-Title': 'Gradle',
                'Implementation-Version': '${version}',
                'Class-Path': '. config/'
        )
    }
}